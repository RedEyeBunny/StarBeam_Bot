File Handling process for screenshots - if screenshot already exist replace it with a new one(if a
                                        new request is made).
                                      - delete all screenshots at the end of each week.
